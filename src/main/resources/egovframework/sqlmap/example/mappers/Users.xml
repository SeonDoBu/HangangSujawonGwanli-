<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="egovframework.example.sample.service.impl.UsersMapper">
	<select id="getUserById" parameterType="Users" resultType="Users">
		SELECT *
		FROM   users
		WHERE  status     = '1'
		<if test="small_code != 0">
		AND    small_code = #{small_code}
		</if>
		<if test="user_id != '' and user_id != null">
		AND    user_id    = #{user_id}
		</if>
		<if test="password != '' and password != null">
		AND    password   = #{password}  
		</if>
	</select>

	<select id="checkDuplicateId" parameterType="String" resultType="int">
		SELECT count(*)
		FROM   users
		WHERE  user_id = #{id}
	</select>
	
	<insert id="joinUser" parameterType="Users">
		INSERT INTO users(user_id,big_code,small_code,gigwan_id,password,name,email,tel,dept,position,status)
		VALUES(#{user_id},1,#{small_code},#{gigwan_id},#{password},#{name},#{email},#{tel},#{dept},#{position},'1')
	</insert>
	
</mapper>